node_modules/
**/node_modules/
**/dist/
**/.turbo/
pnpm-lock.yaml
package-lock.json
*.tsbuildinfo
coverage/
apps/**/dist/

# Generated API types (regenerated by `pnpm api:types`, gitignored)
packages/api-types/src/generated.ts

# OpenAPI snapshot — written by apps/api/scripts/dump-openapi.ts (multi-line JSON
# is the source of truth that the snapshot test compares against). Letting Prettier
# reformat it to inline arrays would break the test and the contract-drift CI gate.
packages/api-types/openapi.json

# Tool-managed directories (OpenSpec CLI + Claude Code skills own the formatting)
.claude/
openspec/
